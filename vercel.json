{
  "buildCommand": "cd frontend && npm install && npm run build && node -e \"require('fs').cpSync('dist', '../public', {recursive: true})\" && cd ../backend && npm install",
  "outputDirectory": "public",
  "functions": {
    "api/server.js": {
      "includeFiles": "backend/**"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/server" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
