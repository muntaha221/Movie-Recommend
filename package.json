{
  "name": "movie-recommend-root",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "cd backend && npm install",
    "build": "cd frontend && npm install && npm run build && node -e \"require('fs').cpSync('dist', '../public', {recursive: true})\""
  }
}
